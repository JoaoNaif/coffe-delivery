import styled from 'styled-components'

export const CheckoutContainer = styled.main`
  .form-container {
    display: flex;
    gap: 1.5rem;

    margin-top: 3rem;

    .input-container {
      flex: 1;
    }

    .value-container {
      flex: 1;
    }

    .title-check {
      font-size: 1.125rem;
      font-family: 'Baloo 2';
      margin-bottom: 0.5rem;
    }
  }
`
